namespace = albion_init

albion_init.1 = {
	type = country_event
	placement = ROOT
	title = albion_init.1.tt
	desc = albion_init.1.dd
	flavor = albion_init.1.ff

	trigger = {
		NOT = { has_global_variable = oberon_happened_globvar }
	}

	immediate = {
		random_scope_character = {
			limit = {
				has_template = oberon_character_template
			}
			save_scope_as = oberon_scope
		}
	}

	option = {
		name = albion_init.1.a
		default_option = yes

		scope:oberon_scope = {
			set_character_as_ruler = yes
		}
	}
}
