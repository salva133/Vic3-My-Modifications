name: Trim Trailing Whitespace

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  trim-whitespace:
    if: github.event.pull_request.merged == true
    uses: salva133/My-Workflows/.github/workflows/trim-trailing-whitespace.yml@a6dca982fcd1697e4befbb64cafa1e33a331fe4c